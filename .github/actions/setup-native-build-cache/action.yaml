name: Setup native build cache
description: Restore native package build caches for the current platform
inputs:
  platform:
    description: Native package platform
    required: true
runs:
  using: composite
  steps:
    - name: Cache Cargo build
      if: inputs.platform == 'win32' || inputs.platform == 'android'
      uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git
          rust/target
        # The commit sha makes every commit write a fresh entry (actions/cache
        # skips saving on an exact key hit); restore-keys warms from the newest
        # prior entry.
        key: cargo-${{ inputs.platform }}-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust/Cargo.lock', 'rust-toolchain.toml') }}-${{ github.sha }}
        restore-keys: |
          cargo-${{ inputs.platform }}-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust/Cargo.lock', 'rust-toolchain.toml') }}-
          cargo-${{ inputs.platform }}-${{ runner.os }}-${{ runner.arch }}-
